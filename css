.card {
  background: black;
  border-radius: 12px;
  padding: 20px;
  box-shadow: 0 6px 15px rgba(0,0,0,0.1);
  transition: transform 0.2s ease;
}

.card:hover {
  transform: translateY(-5px);
}
.hero h2 {
  color: #EADA09;
  font-weight: bold;
}

.hero p {
  color: #FFFFFF;
}
